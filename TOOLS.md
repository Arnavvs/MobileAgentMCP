# Tool catalogue

Generated by `python scripts/tools_catalogue.py --write` - do not edit by hand.

An MCP client receives all of this automatically at handshake; this file exists so a human can browse the surface without starting a session.

**73 tools** across 13 groups.

## Device & connection

| Tool | Arguments | Description |
|---|---|---|
| `device_info` | `()` | Model, Android version, screen size, density, battery. |
| `devices` | `()` | List connected devices and their state. |
| `foreground_app` | `()` | Package and activity currently in the foreground. |
| `launch_app` | `(package, wait_seconds?)` | Launch an app. Accepts a package name or an alias (instagram, reddit, chrome, twitter, settings). |
| `list_apps` | `(filter?, limit?)` | List installed packages, optionally filtered. |
| `stop_app` | `(package)` | Force-stop an app. Use before relaunching when you need a fresh start: `am start` on a live task only refocuses it. |

## Screen reading

| Tool | Arguments | Description |
|---|---|---|
| `extract_fields` | `(app?, screen?)` | Extract clean typed fields for a recognised screen via the versioned selector registry. Numbers arrive as {raw, value} so a parse can be audited. Missing fields are listed in `_unavailable` rather than guessed. |
| `find_element` | `(query?, resource_id?, clickable_only?, limit?)` | Search the current screen for elements matching text or a resource-id. Re-dumps first, so results are always fresh. |
| `screenshot` | `(name?)` | Take a screenshot and save it, returning the PATH (not the image). Expensive next to ui_dump - use only when pixels genuinely matter, e.g. content the accessibility tree cannot express. |
| `ui_dump` | `(query?, clickable_only?, limit?, include_system?)` | Read the current screen as structured elements. This is the primary way to see the device - use it instead of screenshot. Returns elements with index `i` (for tap), resource-id, text, content-desc, tap centre `c`, and flags `f` (C=clickable S=scrollable *=selected). Also names the screen and reports selector drift for known apps. |

## Input

| Tool | Arguments | Description |
|---|---|---|
| `long_press` | `(i?, x?, y?, duration_ms?)` | Long-press an element or coordinate, e.g. to open a context menu. duration_ms defaults to 700. |
| `press_key` | `(key)` | Press a hardware/navigation key: back, home, enter, recents, wake, sleep, delete, search, volume_up/down. |
| `swipe` | `(direction?, x1?, y1?, x2?, y2?, duration_ms?)` | Swipe. direction: up\|down\|left\|right, or give explicit x1,y1,x2,y2. `up` advances content (next reel/post). |
| `tap` | `(i?, x?, y?)` | Tap the screen. Give either `i` (element index from the last ui_dump) or explicit x/y. `i` is preferred: it is resolution independent and self-documenting. |
| `text_input` | `(text)` | Type text into the focused field. Tap the field first. |

## System / phone

| Tool | Arguments | Description |
|---|---|---|
| `battery_status` | `()` | Battery, charging state, temperature and health. |
| `clipboard_get` | `()` | Read the clipboard. Requires the uiautomator2 agent, since Android restricts clipboard reads to the foreground app and `cmd clipboard` is unavailable on many OEM builds. |
| `clipboard_set` | `(text)` | Set the clipboard contents. |
| `list_files` | `(path?, limit?)` | List files in a directory on the device. |
| `media_volume` | `(level?)` | Set or read media volume. Omit `level` to read. level is 0..max. |
| `network_info` | `()` | Wi-Fi SSID, IP address and connectivity state. |
| `notifications` | `(limit?, package?)` | Read currently posted notifications (package, title, text). Read-only; does not dismiss or act on anything. |
| `open_url` | `(url)` | Open a URL in the device's default browser via an intent. |
| `pull_file` | `(remote_path, name?)` | Copy a file from the device to the host artifacts directory. Returns the local path. |
| `push_file` | `(local_path, remote_path)` | Copy a local file onto the device. |
| `screen_power` | `(action?)` | Turn the screen on or off. action: on\|off. Does not unlock. |
| `screen_state` | `()` | Whether the screen is on, and whether it is locked. |
| `scroll_until` | `(query, direction?, max_swipes?, settle_s?)` | Swipe repeatedly until `query` appears or max_swipes is reached. Useful for reaching an off-screen item in a long list. |
| `storage_info` | `()` | Free/used space on internal storage, and RAM. |
| `wait_for` | `(query?, resource_id?, timeout_s?, poll_s?)` | Poll until an element matching `query` or `resource_id` appears, or timeout. Returns the matching elements. Use instead of blind sleeps after an action that triggers loading. |

## Agent learning

| Tool | Arguments | Description |
|---|---|---|
| `collect_feed` | `(field?, max_items?, max_swipes?, settle_s?, app?, screen?)` | Scroll a feed and collect items, deduplicated. Works on any screen whose registry entry has a `repeating` field, so it is not tied to one app. Stops on max_items, max_swipes, or when scrolling stops yielding anything new. |
| `diff_after_action` | `(action?, settle_s?)` | Snapshot the screen, perform an action, snapshot again, and report which anchors CHANGED value. On a feed this identifies the content fields automatically: what changes when you scroll is content, what stays is chrome. action: swipe_up\|swipe_down\|swipe_left\|swipe_right\|none. |
| `explore_screen` | `(max_anchors?, value_chars?)` | Analyse an UNKNOWN screen and report its structure: which anchors hold which values, which repeat (list items), and which are interactive. Use this first when meeting an app that is not yet in the selector registry - it is the discovery step before propose_selectors. |
| `learn_screen` | `(app, screen, fields, requires?, note?)` | Persist an agent-authored screen definition into the selector registry, pinned to the current app version. This is how the agent teaches itself a new app: explore -> diff -> propose -> learn. |
| `propose_selectors` | `(app?, screen?, content_anchors?)` | Draft a selector spec for the current screen from what is visible. Returns a candidate field map you can review and pass to learn_screen. Anchors seen to change across a diff should be marked as content; pass them via `content_anchors` to prioritise them. |

## Threads & comments

| Tool | Arguments | Description |
|---|---|---|
| `collect_thread` | `(max_comments?, max_swipes?, settle_s?, nested?, app?, screen?)` | Collect a comment thread with nested replies. Scrolls, pairs each header with the body nodes that follow it, deduplicates, and returns either a nested tree or a flat list. Requires the screen to have a `thread` spec in the selector registry - use explore_screen + learn_thread on an unmapped app first. |
| `learn_thread` | `(app, screen, header_anchor, body_anchor, footer_anchor?, ad_anchor?, composer_anchor?, level_pattern?, header_composite?, requires?)` | Teach the registry how a comment screen is structured, so collect_thread works on it. Give the anchors for the comment header and body, and a regex whose first group is the nesting level if the app declares one. |

## Selector registry

| Tool | Arguments | Description |
|---|---|---|
| `check_drift` | `(app?, screen?)` | Check whether the live app UI still matches the recorded selector baseline. Run after an app update, or when extraction starts returning nulls. |
| `record_baseline` | `(app?, screen?)` | Record the current screen's resource-ids as the baseline for this app version. Use after verifying a new version, so later drift checks have something to compare against. |
| `registry_info` | `(app?)` | Show the selector registry for an app: known versions, screens and defined fields. |

## Instagram - core

| Tool | Arguments | Description |
|---|---|---|
| `reset_reels_feed` | `(settle_seconds?)` | Re-enter the Instagram Reels tab. Recovers from the first-reel bug (overlay renders with no counts/caption) and re-seeds the feed. |

## Instagram - profile

| Tool | Arguments | Description |
|---|---|---|
| `ig_about_reel` | `(wait_s?, poll_s?, close_after?)` | Read Instagram's own AI-generated 'About this reel' description - Meta's content understanding of the reel, a strong metadata signal. Opens the More sheet, waits for the text to generate, reads it, and closes with BACK. |
| `ig_collect_posts` | `(max_posts?, max_swipes?, settle_s?, open_first?)` | Collect posts from the post FEED. Opens the first grid post once, then scrolls the resulting feed - far cheaper than returning to the grid per post. Captures type, location, audio, likes, comments, reposts, carousel size, caption and date. |
| `ig_collect_reel_details` | `(indices, poll_s?, overlay_timeout_s?, skip_settle_s?, max_swipes?, expect_handle?)` | Open reels from the grid and extract each one's detail, swiping through in grid order and capturing only the requested indices. Reuses the reels_viewer selectors already in the registry. |
| `ig_open_profile` | `(handle, settle_s?)` | Open an Instagram profile by handle using a deep link, which skips the search flow. Returns how long navigation took. |
| `ig_plan_reel_sample` | `(reels, latest?, top?, mid?, low?)` | Plan which reels to open from a reels-grid scan, per the sampling spec: N latest plus top/mid/low view tiers drawn from the same sample. Returns deduplicated grid indices - overlaps between 'latest' and 'top' are opened once. |
| `ig_profile_stats` | `()` | Scrape the profile header: display name, followers, following, post count, bio, verified and follow state. Numbers are returned as integers alongside their raw strings. |
| `ig_scan_grid` | `(max_items?, max_swipes?, settle_s?)` | Read the profile GRID and classify every tile as post or reel without opening anything. Grid tiles self-describe: 'Reel by X at row..' vs 'N photos and M videos by X at row..'. Scrolls to gather up to max_items. |
| `ig_scan_reels_grid` | `(max_reels?, max_swipes?, settle_s?, expect_handle?)` | Read the REELS tab grid, returning each reel's view count without opening it. This is what makes view-tier sampling cheap: rank in code rather than tapping a sort control (none exists for other users' profiles). |

## Instagram - comments

| Tool | Arguments | Description |
|---|---|---|
| `ig_close_sheet` | `()` | Close the comment sheet (or any Instagram bottom sheet) with a single back press. |
| `ig_collect_reel_comments` | `(max_comments?, max_swipes?, settle_s?, open_sheet?)` | Open the comment sheet on the current reel/post and collect comments. Scrolls the sheet (upward only - swiping down dismisses it), assembles username/text/date/likes, and reports collapsed reply counts. Read-only; never types. Requires a reel or post to be open first. |

## Instagram - capture

| Tool | Arguments | Description |
|---|---|---|
| `ig_capture_cut` | `(name)` | Cut to a new segment: finalize the current reel's file and begin recording the next under `name`. Call this once per reel, right after the reel is on screen. Names become <name>.mp4. |
| `ig_capture_list` | `()` | List the recorded segment files on the device with sizes. |
| `ig_capture_pull` | `(subdir?)` | Copy all recorded segment MP4s to the host artifacts directory. Returns local paths and sizes. |
| `ig_capture_reel_full` | `(name, max_s?, fallback_s?, poll_s?, min_s?)` | Record the CURRENT reel in full: cut a new segment, then dwell until the reel loops back to the start (detected by the scrubber playback position wrapping) rather than for a fixed guess. Falls back to fallback_s when the scrubber is unavailable, and never exceeds max_s. Call once per reel, then swipe and call again. |
| `ig_capture_start` | `(clear_previous?)` | Start ONE segmented capture session. Launches the capture app and requests MediaProjection consent - the ONLY manual tap in the whole run. After the operator taps 'Start now', every reel is recorded to its own file with no further prompts. Poll ig_capture_status until ready before cutting. |
| `ig_capture_status` | `()` | Check whether the segmented capture session is live yet (after the consent tap). Returns state ready\|waiting\|failed. |
| `ig_capture_stop` | `()` | Stop the capture session: finalize the last segment and release the projection. Call once at the end of a run. |

## Instagram - web/loader

| Tool | Arguments | Description |
|---|---|---|
| `ig_web_comments` | `(shortcode, limit?, include_replies?)` | Fetch comments for a post/reel by shortcode, including replies where Instagram exposes them. Far more complete than the on-device sheet, which is viewport-limited to roughly 7 visible comments. |
| `ig_web_download` | `(shortcode, subdir?)` | Download a reel's video by shortcode straight from Instagram's CDN to local disk. Bypasses the phone entirely - no MediaProjection, no ADB transfer at 2.54 MB/s. |
| `ig_web_import_session` | `(cookie_path?)` | Adopt an EXISTING Instagram session from InstaScrape's cookie.json instead of logging in. Strongly preferred over ig_web_login: a fresh login from a new IP triggers Instagram's checkpoint, whereas reusing already-validated cookies does not. Saves the imported session so later runs resume without re-importing. |
| `ig_web_login` | `(force?)` | Log in to Instagram using credentials from .env and persist the session to disk, so later runs resume without re-authenticating. Called automatically by the fetch tools when a session expires. |
| `ig_web_profile` | `(handle)` | Fetch an Instagram profile without the device: followers, following, post count, bio, external link, verified and private flags. Typically ~1s versus ~12s for the on-device path. |
| `ig_web_reels` | `(handle, limit?, include_video_url?)` | List a profile's reels with metadata AND direct video URLs - no device, no playback, no recording. Returns shortcode, caption, views, likes, comments, duration and video_url per reel. This is the fast replacement for watching reels on the phone. |
| `ig_web_status` | `()` | Check whether the saved Instagram web session is still valid. Does NOT log in. Use before a run so expiry is discovered up front rather than mid-scrape. |

## X / Twitter

| Tool | Arguments | Description |
|---|---|---|
| `x_collect_multi` | `(names, max_tweets?, max_swipes?)` | Scrape several X timelines in one pass: switch to each named timeline, collect tweets, and return them grouped by timeline. Returns to the top of each before collecting so runs are comparable. |
| `x_collect_replies` | `(i?, max_replies?, max_swipes?, settle_s?)` | Open the tweet currently at the top of the timeline (or index `i` from the last ui_dump) and collect its reply thread. Replies are returned in order with an inferred depth; X does not declare depth the way Reddit does, so depth is approximate and labelled as such. |
| `x_collect_timeline` | `(max_tweets?, max_swipes?, settle_s?, include_ads?)` | Collect tweets from the current timeline, scrolling and deduplicating. Captures handle, display name, verified, age, text, links, media type, ad flag, and metrics including IMPRESSIONS. |
| `x_list_timelines` | `()` | List the timeline tabs currently available on X - 'For you', 'Following', and any custom timelines/Lists the account has pinned. Use before x_switch_timeline to see what exists. |
| `x_open` | `(tab?, wait_s?)` | Open X/Twitter. tab: for_you \| following. Returns the active tab so you can confirm which timeline you are on. |
| `x_scroll_to_top` | `()` | Jump to the top of the X timeline. Uses the 'Scroll to top' affordance if present, else taps the Home tab (which also returns to top when already on Home). Far cheaper than swiping down repeatedly, and avoids over-scrolling into a refresh. |
| `x_search` | `(query, tab?, max_tweets?, max_swipes?)` | Keyword search on X. Opens Explore, types the query, submits, and optionally switches result tab (top\|latest\|people\|media). Returns the collected result tweets. |
| `x_switch_timeline` | `(name, settle_s?)` | Switch to a named timeline tab on X (e.g. 'Following', or a custom timeline such as 'Soccer'). Matches case-insensitively on the tab label. Follow with x_collect_timeline to scrape whatever is active. |

---

`?` marks an optional argument. Descriptions are the first line only; the full text is what the agent actually receives.
